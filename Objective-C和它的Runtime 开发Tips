# Objective-C和它的Runtime 开发Tips

- Objective-C采用消息结构而非函数调用，这两者的关键区别是：使用消息结构的语言，其运行时所应执行的代码由运行环境来决定，而使用函数调用的语言则是由编译器决定的。如果被调用的函数是多态的，那么就要在运行时按照“虚方法表”来查出到底该执行哪个函数。dynamic binding

- Obj-c运行时才会检查对象类型，接受一条信息后，该执行什么代码并非由编译器决定而是在运行时才决定

- static const 声明变量就相当于在该文件中使用define，唯一的区别是这种变量的生命方式保存了类型信息。

- 