#关于反编译和反汇编的那些事

##常见的反汇编算法

- 线性扫描
- 递归下降

## 线性扫描

原则:一条指令结束的地方便是另一条指令的开始。

优点:他能完全覆盖程序的所有代码段

缺点：无法正确地将嵌入的数据和代码区分出来

## 递归下降

原则：强调控制流，控制流根据一条指令是否被另一条指令引用来决定是否对其进行反汇编。

优点：清楚代码的控制流，具有区分数据与代码的强大的能力。

缺点：如果遇到无条件分支指令，则很难确定跳转指令的目标，也就无法确定该从什么地方继续反汇编过程。
